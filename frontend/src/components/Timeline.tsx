/**
 * Timeline.tsx — Session event timeline visualization.
 *
 * Shows a horizontal timeline of the session with markers for:
 *  - Good reps (green)
 *  - Form issues (amber warning markers)
 *  - Session phases
 *
 * Renders using Recharts.
 *
 * IMPLEMENTED IN STEP 10.
 */

export default function Timeline() {
  return null
}
